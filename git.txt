one time in any machine
install git
check git in local machine: git --version
git config --global user.name "Abul Khair"
git config --global user.email "devkhair31@gmail.com"
should check:git config --global user.name

Required for each project:
*git init
* git add . ( if all root files selected)


git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/devkhair31/assignment.git
git push -u origin main